import React from "react";

export interface ButtonM{
    children?: React.ReactNode;    
    bg?: string;
    color?: string;
    fontSize?: string;
    padding?: string;
    borderRadius?: string;
    text?: string;
    border?: string;
}